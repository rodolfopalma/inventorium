doctype html
html
    head
        include ./includes/head.jade

    body
        include ./includes/navbar.jade

        section#login_form

            .container
                h2.col-md-6.col-md-offset-3.text-center Ingrese a BigSales

                if invalidFields
                    ul.col-md-6.col-md-offset-3.bg-danger.list-unstyled
                        each invalidField in invalidFields
                            if invalidField == "inputName"
                                li Ingrese un nombre válido.
                            if invalidField == "inputEmail"
                                li Ingrese un correo electrónico válido.
                            if invalidField == "inputPassword"
                                li La contraseñas no coinciden o no son válidas.

                form.col-md-6.col-md-offset-3(action="/login", method="POST")
                    .form-group
                        label(for="input-email") Ingrese su email
                        input(type="email", class="form-control", name="inputEmail", id="input-email", placeholder="jose@perez.com")
                    .form-group
                        label(for="input-password") Ingrese su contraseña
                        input(type="password", class="form-control",  name="inputPassword", id="input-password")
                    .form-group
                        button(type="submit", class="btn btn-default") Ingresar

        
        include ./includes/footer.jade
        include ./includes/scripts.jade
