doctype html
html
    head
        include ./includes/head.jade

    body
        include ./includes/navbar.jade

        section#singup_form

            .container
                h2.col-md-6.col-md-offset-3.text-center Inscríbase en BigSales

                p.col-md-6.col-md-offset-3.text-justify.
                    Está a un sólo paso de disfrutar nuestros servicios, que le permitirán
                    sacarle el máximo provecho a sus inventarios y dejar de perder dinero por malas predicciones.

                if invalidFields
                    ul.col-md-6.col-md-offset-3.bg-danger.list-unstyled
                        each invalidField in invalidFields
                            if invalidField == "inputName"
                                li Ingrese un nombre válido.
                            if invalidField == "inputEmail"
                                li Ingrese un correo electrónico válido.
                            if invalidField == "inputPassword"
                                li La contraseñas no coinciden o no son válidas.

                form.col-md-6.col-md-offset-3(action="/signup", method="POST")
                    .form-group
                        label(for="input-name") Ingrese su nombre
                        input(type="text", class="form-control", name="inputName", id="input-name", placeholder="José Pérez")
                    .form-group
                        label(for="input-email") Ingrese su email
                        input(type="email", class="form-control",  name="inputEmail", id="input-email", placeholder="jose@perez.com")
                    .form-group
                        label(for="input-password") Ingrese su contraseña
                        input(type="password", class="form-control",  name="inputPassword", id="input-password")
                    .form-group
                        label(for="input-confirm-password") Confirme su contraseña
                        input(type="password", class="form-control",  name="inputConfirmPassword", id="input-confirm-password")
                    .form-group
                        button(type="submit", class="btn btn-default") Enviar

        include ./includes/scripts.jade