doctype html
html
    head
        include ../includes/head.jade

    body
        include ../includes/navbar.jade

        section#crear_prediccion_form

            .container
                h2.col-md-6.col-md-offset-3.text-center Agregar Producto a Bodega

                form.col-md-6.col-md-offset-3(action="#{baseUrl}/producto/crear", method="POST", enctype="multipart/form-data")
                    .form-group
                        label(for="input-name") Nombre del producto
                        input.form-control(type="text", id="input-name", name="inputName", placeholder="Zapatos Café")

                    .form-group
                        label(for="input-stock") Ingrese stock actual del producto
                        input.form-control(type="text", id="input-stock", name="inputStock", placeholder="310")

                    .form-group
                        label(for="input-file") Planilla con datos de ventas
                        input(type="file", id="input-file", name="inputFile")
                        p.help-block.
                            La planilla debe tener una sola columna de información,
                            la primera fila debe tener el valor ´Ventas´ y el resto
                            de las filas deben contener los valores de las demandas
                            mensuales del producto.
                    .form-group
                        button(type="submit", class="btn btn-default") Agregar

         
        include ../includes/footer.jade
        include ../includes/scripts.jade
